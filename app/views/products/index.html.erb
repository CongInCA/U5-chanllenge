<h1>Products</h1>
<ul>
  <% @products.each do |product| %>
      <li>
        <a href="<%= product_path(product) %>">

        <%= "#{product.title}, #{product.description}, #{product.price}, #{product.stock_quantity}" %>

        </a>
      </li>
  <% end %>
</ul>